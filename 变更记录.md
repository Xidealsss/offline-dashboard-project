# 变更记录

> 约定：每次提出修改要求后，先在本文件追加记录，再修改代码，最后重新生成 `项目看板.html`。

| 日期 | 修改要求 | 实际变动 | 涉及文件 |
| --- | --- | --- | --- |
| 2026-08-07 | 任务1：取消连线功能 | 移除甘特图依赖箭头渲染（SVG 连线、箭头 marker、任务定位变量及相关样式），保留「前置任务」列的数据解析与校验（存在性、无环），仅去掉展示线 | build_dashboard.py、README.md |
| 2026-08-07 | 任务2：整理排版，修正甘特图出现两个“任务清单”表头 | 左列只渲染「任务清单」表头，右列只渲染时间刻度（周/日）表头，不再克隆完整表头，两列高度保持 52px 对齐 | build_dashboard.py |
| 2026-08-07 | 任务3：里程碑项目排版错位调整 | 里程碑菱形改为锚定在「计划结束」当天的日格中心，不再取整个日期段中点（避免落在两天之间）；左右两列任务行高度统一固定为 52px，消除逐行 1px 漂移导致的错位；任务名过长时省略号截断 | build_dashboard.py |
| 2026-08-08 | 任务4：看板排版存在错位，对整体进行完善 | ①**时间刻度表头**：周标签原来挤在 28px 的周一格里换行，与日号叠字——改为按周合并成 7 格宽的整段（末尾残周按实际天数），日号改为逐日显示、每月 1 号显示「N月」。②**表头与泳道网格线差 1px**：表头日格原用 `border-left`（线在格首）、泳道用 27→28px 渐变（线在格尾），改为统一用 `inset 1px 0` 与 0→1px 渐变，线全部落在格首。③**周末底色**：撤掉覆盖在时间轴上的绝对定位遮罩（它依赖硬编码 top:52px，还会盖在阶段分组条上形成脏条纹），改为泳道背景的第二层渐变，按 196px（7 天）平铺并用 `--wk-x` 对齐首个周六，与表头周末格严格同相。④**两列高度**：行高/分组高/表头高全部收敛到 `--row-h/--group-h/--head-h` 变量，行高 52→56px 解决标签区 39px 内容被 38px 内容框裁切；去掉只在左列生效的 `.row:last-child` 去边框规则。⑤**时间轴列显式定宽** `--tl-w`，甘特改 flex 布局，左侧任务列 `sticky left`、表头 `sticky top`，横向滚动时任务名与刻度不再脱节；容器限高 72vh。⑥今日线锚点由硬编码 52px 改为 `var(--head-h)`，日期标签移入表头刻度区（原来压在第一条任务行上），线宽居中修正 1px。⑦条形/菱形改 `top:50% + translateY(-50%)` 居中，不再依赖行高常量；里程碑增加浅色区间条，避免整行看起来空白。⑧**指标卡**由 `auto-fit` 改固定 8/4/2 列，消除 5+3 断行；**柱状图**由 `auto-fit` 改固定 3 列（窄屏单列并限宽），消除第 3 张被拉成双宽；**切片器**改栅格布局并把「全选/清空」提到标题行右侧，消除各组按钮高低参差。⑨**透视表**表头首格补 `sticky left`（原来只有行表头冻结，横滚时表头与行名脱节），并补 `sticky top`、改 `border-collapse: separate` 保证冻结时边框不丢。⑩筛选/点开详情重渲染时保留甘特滚动位置，首次渲染自动定位到今天。⑪**修复「里程碑完成率」指标卡整张丢失**：该卡类名为 `stat milestone`，与甘特里程碑菱形的 `.milestone` 撞名，被套上 `position:absolute + rotate(45deg) + 14px` 而脱离栅格，只在页面左侧留下一个旋转的绿色「0%」——指标卡实际只显示 7 张。卡片类名改为 `msrate`，同时把菱形样式收进 `.lane` 作用域。 | build_dashboard.py、README.md |
| 2026-08-08 | 任务5-阶段1：可测试化重构（不改行为） | 前端从 `build_dashboard.py` 的原始字符串里拆出 `assets/template.html` + `dashboard.css` + `dashboard.js`，构建时内联，产物仍是单文件自包含 HTML；模板用真实外链标签，可直接双击打开调试，新增 `--dev` 写出 `assets/data.js`。错误模型重构：引入 `Issue`/`Report`/`DashboardError`，库函数不再 `sys.exit`，**多类错误一次全报**（旧版重复ID/前置缺失/环各自中断，用户得修一个跑一次）；退出码分档 0/1/2；顶层兜底避免原始 traceback 弹进 `.command` 窗口；本行已报错的任务不再引发下游「前置不存在」连锁误报。**验收：拆分前后产物字节完全一致。** | build_dashboard.py、assets/* |
| 2026-08-08 | 任务5-阶段2：测试基线 | 新增 `tests/`，用**标准库 unittest**（不引入 pytest，避免给「双击就能跑」的项目加装依赖）：解析/校验/环检测/CPM/端到端/退出码 + Playwright 浏览器回归（未装 playwright 自动跳过）。浏览器侧固化了几何对齐、**类名撞车守卫**、规则引擎矩阵、5 档响应式、交互联动、零 console error。新增 `运行自检.command`。**验收：把 `.stat` 类名改回 `milestone`，UI 测试确实失败**（`['static','absolute'] != ['static']`），证明这套测试抓得住上次那个 bug。 | tests/*、运行自检.command |
| 2026-08-08 | 任务5-阶段3：健壮性护栏与校验增强 | 护栏：时间轴跨度 >1095 天 / 行数 >800 报错并**指出是哪一行撑开的**（年份手误 2226 会撑出 7 万天时间轴、14 万 DOM 节点）；公式单元格 `data_only` 双读，只有「是公式且无缓存值」才报错并给出可操作提示；环检测由递归改**显式栈迭代**，消除逆序长链的 `RecursionError`（实测 1200 级即崩，现已过 5000 级）。校验：读取此前完全被忽略的**「选项字典」页**校验枚举值（默认 warning，`--strict` 升级为 error）；一致性提示（状态与进度矛盾、任务落在项目起止之外、依赖倒挂）；搭接不逐条刷屏而是汇总一条。看板顶栏新增「数据提示 N」可展开面板。`运行看板.command` 去掉写死的个人绝对路径、增加 PEP 668 下的 `.venv` 兜底、不再吞掉真实报错。 | build_dashboard.py、assets/*、运行看板.command |
| 2026-08-08 | 任务5-阶段4：能力增强 | **关键路径与浮动时差**：「前置任务」此前只用于校验无环，现在在 Python 侧做 CPM 正推/逆推（**按日历日**；「工作日」配置仍只作展示，不参与推算），payload 带出 `es/ef/ls/lf/tf/critical`；甘特关键链加深红描边与「关键」标记，非关键行显示浮动天数，切片器与透视表新增「关键路径」维度、透视值新增「平均浮动(天)」，详情面板补六项 CPM 字段，副标题给出关键链工期。**甘特日/周/月缩放**：位置统一由「距首日天数 × 每日像素」推算，表头分段宽度同源，三档粒度下刻度与条形严格对齐；表头按粒度缓存，筛选时不再重建整条时间刻度；切粒度保持视口中心日期。**搜索 + URL 状态**：搜索任务/负责人/交付物等字段并联动全部组件；筛选/搜索/粒度/透视维度/选中任务编码进 hash，可收藏分享，`?today=` 不受影响。顺带修掉两个由新粒度暴露的缺陷：`.day-row .day` 缺 `nowrap` 导致粗粒度标签折行溢出到分组条；刻度标签的显隐阈值改为按标签字数估宽。 | build_dashboard.py、assets/*、tests/* |
| 2026-08-08 | V3.0：本地操作台（网页式）+ Mac 应用打包 + Windows 预留 + 归档 V3.0 | **新增网页式本地操作台**：`看板操作台.py` 用标准库 `http.server` 起本地服务（**仅绑定 127.0.0.1、端口自动选空闲**，完全离线），`webbrowser.open` 自动打开操作台页面；接口为 `GET /`（操作台页）、`POST /api/generate?filename=`（**原始字节直传**，复用 `build()` 校验/CPM/统计，产物经 `render_html`+`load_assets` 生成自包含 HTML）、`GET /api/dashboard?session=`（查看）、`GET /api/download?session=`（下载，UTF-8 文件名）、`GET /api/selfcheck`（环境自检）、`POST /api/quit`（退出）；上传限 100MB、仅 `.xlsx/.xlsm`、会话保留最近 10 份；看板自动保存到 `~/项目看板操作台/项目看板.html`；新增 `--smoke-test` 非交互自检（首页/生成/产物三连验）。**新增操作台页面** `console.html`（中文按钮：选择文件、生成分析、打开看板、下载 HTML、环境自检、退出；错误/警告按现有中文提示展示）。**新增打包体系**：`看板操作台.spec`（同一 spec，环境变量 `KANBAN_ONEFILE=1` 时出 Windows onefile exe，否则出 Mac onedir app）、`打包Mac应用.command`（`.build-venv` 安装 pyinstaller/openpyxl 后打 `dist/看板操作台.app` 与 `看板操作台_Mac.zip`）、`打包Windows应用.bat`（**本次只写不跑**，待 Windows 机器执行）、`使用说明.md`（最终用户一页说明 + Windows 验收清单）。**新增测试** `tests/test_console.py`（10 项：首页、正常生成、坏文件、扩展名、超大上传、会话上限、下载头、自检、smoke-test、退出）。**文档更新**：README 增“本地操作台”章节与文件说明；PLAN 增 V3.0 完成说明。**既有逻辑不变**：`build_dashboard.py`、CLI、`运行看板.command`、`运行自检.command` 均未改动；V2.0 目录原样保留。**验收**：既有 110 项测试 + 新增 10 项全部通过；Mac app 构建后 `--smoke-test` 通过；归档核对 V3.0 快照、`离线看板进阶_V3.0.zip`、`交付物/看板操作台_Mac.zip` 内容一致。 | 看板操作台.py、console.html、看板操作台.spec、打包Mac应用.command、打包Windows应用.bat、使用说明.md、tests/test_console.py、README.md、PLAN.md |
| 2026-08-08 | 任务6：阅读文件内容，确定变更内容与迭代路线 | **只读盘点，未改任何代码与产物**。通读 PLAN / README / 变更记录 / 使用说明 四份文档，并核对 `build_dashboard.py`、`看板操作台.py`、`assets/dashboard.js`、`看板操作台.spec`、双平台打包脚本与快照 zip 的实际状态，梳理出 MVP →（阶段一至四）→ V3.0 的完整变更脉络。**补记两处此前漏账的动作**：①当天 22:53 又执行过一次 Mac 打包，`dist/看板操作台.app` 内嵌 Python 由 3.12 变成 3.14（主程序 2,365,856 → 2,674,192 字节），但 `交付物/` 里仍是 17:46 那一版，两个 `看板操作台_Mac.zip` 也不一致（10.98MB / 10.58MB）——**当前对外分发物落后于最新构建**；②22:54 重新生成过 `项目看板.html`，而 `离线看板进阶_V3.0.zip`（17:48）里仍是 17:02 版。**确定 V3.1 主线为「规模与性能」**（PLAN 新增第 8 节）：目标把容量从「≤500 行设计 / 800 行护栏」提到 2000 行 × 5 年，六项瓶颈已定位到具体函数——`renderPivot` 逐格 `list.filter` 全表扫描（负责人×阶段 800 行下约 32 万次比较/重绘）、`renderGantt` 每次 refresh 全量重建、`buildHeader` 日视图 1250 个刻度节点/30660px 宽、`build()` 的 `load_workbook` 双读且未 `read_only`、`data_script` 每任务 26 字段含 6 个 CPM 日期字符串、护栏阈值靠挡不靠扛；执行顺序定为「先建 `tests/test_perf.py` 基准 → 低风险的算法/IO 项 → 渲染结构项 → 最后放宽护栏」。Windows 打包与签名、工作日制排期、导出 CSV/PDF 明确继续挂起。 | PLAN.md、变更记录.md |
| 2026-08-08 | 任务7：暂不考虑 Windows，继续优化 Mac 端应用，制定后续优化方向 | **仍是只读盘点 + 规划，未改代码**。实测 Mac 产物后新增四项发现：①两个 `.app` 主程序均为 `Mach-O 64-bit arm64` **单架构，Intel Mac 双击只闪退且无任何提示**（构建用 Python 只有 arm64，universal2 需换 python.org 安装包）；②`Info.plist` 的 `CFBundleShortVersionString` 是 PyInstaller 默认的 `0.0.0`，图标也是自带的 `icon-windowed.icns`；③app 23MB 中 `libcrypto`(3.5M)+`libzstd`(2.0M)+`libssl`(632K) 共 6.1MB，而本应用全离线、只跑 127.0.0.1 明文 HTTP，这三个根本用不上；④`.build-venv` 跟随系统 Python 漂移（3.12→3.14），正是交付物与 dist 不一致的根因。**改写 PLAN 第 8 节**：主线仍为看板「规模与性能」（8.2～8.4 六项瓶颈与执行顺序不变）；新增 8.5 Mac 支线，只做**四项运行期硬伤 M1-M4**（M1 Dock 图标点不动/Cmd+Q 无效/关标签后进程僵尸——根因是 `console=False` 进 Dock 却无 Cocoa 事件循环，改法为 `LSUIElement=1` + 页面心跳超时自退；M2 双击两次起两实例、争写同一个 `项目看板.html`，改法为端口文件 + 单实例复用；M3 `_upload.xlsx` 上传副本从不清理，改 `tempfile` + `finally` 删除；M4 arm64 限制在启动与文档中显式提示，本轮不做 universal2）与**五项发布规范 R1-R5**（版本号单一来源、自定义图标、`打包Mac应用.command` 扩成「构建→smoke-test→同步交付物→重打快照→打印校验和」一键流水线、锁定构建用 Python 版本、先对齐当前两版产物）。**Windows 分支下线**：spec 里 `KANBAN_ONEFILE` 分支零成本保留，但 README 与 `使用说明.md` 第 5 节的 Windows 内容待移除。签名公证、universal2、体积裁剪（约可降到 15MB）、工作日制排期、导出 CSV/PDF 均记入 8.6 挂起。 | PLAN.md、变更记录.md |
| 2026-08-08 | 任务8：按计划向下推进（暂不考虑 Windows） | **首轮代码落地，主线 2/6 + Mac 支线 6/9。**【基线】新增 `tests/test_perf.py`：合成 200/500/800/2000 行 × 1/3/5 年数据，测 build 耗时、HTML 体积、首屏、切片器重绘、切透视维度；分「回归护栏」（默认，宽松）与「目标断言」（`KANBAN_PERF_TARGET=1`，即 8.4 验收线）两档，结果落 `tests/perf-baseline.json`。**基准自身修过一次**：初版点一个 chip 就计时，那是「筛下去」、剩的行更少，测出来 92ms 偏乐观；改成先筛再取消、量 2000 行全量回来那一次，真实值 406ms。【瓶颈4 ✅】`build()` 原来无条件把工作簿读两遍（`data_only` 真/假各一次）取公式缓存值，但绝大多数模板一个公式都没有。新增 `CachedWorkbook` 惰性读取器，只有真撞上公式才开第二份，并显式 `close()`；**build 1.02s → 0.56s（2000 行）**。`read_only=True` 实测只省 0.07s 且要踩二次迭代的坑，权衡后未采用。【瓶颈1 ✅】`renderPivot` 原来每个格子一次 `list.filter` 全表扫描、行列合计再各扫一遍、维度去重用 `Array.includes`，改为单遍 `Map` groupby + `Set` 去重（新增 `pivotAcc`/`pivotAdd`，`pivotValue` 改吃累加器）。**顺带发现更大的浪费**：切透视维度走的是整表 `refresh()`，把甘特也重建了一遍——改成只重画透视表，**切透视 1246ms → 4ms**。新增暴力对拍测试，保证聚合改写后每个格子的数字与逐条统计一致。【发现】**切片器仍是 365→406ms（噪声内）**，说明筛选开销压根不在透视表而在 `renderGantt` 全量重建，要达标必须做瓶颈 2 虚拟滚动——基准跑出来的结论，事先猜不到。【M1 ✅】`Info.plist` 加 `LSUIElement=1`（不进 Dock，消除点不动、Cmd+Q 杀不掉的图标）+ 页面每 30s 打 `/api/ping`、服务端看门狗闲置 600s 自退（`--idle-timeout` 可调，0 关闭），根治「关掉标签后进程变僵尸」。【M2 ✅】单实例：启动先探 `~/项目看板操作台/.console.port`，能 ping 通就把浏览器指过去并退出；过期/脏端口文件自动清理。【M3 ✅】上传副本改 `tempfile.mkstemp` + `finally` 删除，不再把用户项目数据以 `_upload.xlsx` 永久留在输出目录。【M4 ✅】自检接口回报 `arch`/`python`/`frozen`；打包脚本提示 arm64 限制并在交付物旁生成 `请先阅读.txt`；文档首行写明仅支持 Apple 芯片 Mac。【R1 ✅】`__version__ = "3.1.0"` 成为唯一来源，spec 用正则读它写进 `CFBundleShortVersionString`/`CFBundleVersion`，`server_version`、页面标题与页脚都从它取（原来 Info.plist 里是默认的 `0.0.0`）。【R3 ✅】`打包Mac应用.command` 从「只构建」扩成流水线：构建 → 冻结包 `--smoke-test` → 同步 `交付物/` → 重打源码快照 → 打印 SHA256 前 16 位，`set -euo pipefail`，任一步失败即中止。【R4 ✅】锁定构建用 Python（`PINNED_PY=3.12`），已有 `.build-venv` 版本不符会提示重建——这正是上一轮 3.12→3.14 漂移的根因。【文档】README 与 `使用说明.md` 移除全部 Windows 内容（`打包Windows应用.bat` 与 spec 里的 `KANBAN_ONEFILE` 分支零成本保留但不再宣传），补 Apple 芯片限制、单实例、自动退出、一键发布说明；PLAN 8.3 回填实测表并标注各项完成状态。**验收：测试从 165 项增至 183 项，全绿**（含 45 项 playwright 浏览器回归）。【待办】瓶颈 2/3/5/6、R2 自定义图标、R5 重新打包同步交付物——R5 需在 Mac 上执行，容器跑不了 PyInstaller。 | tests/test_perf.py、tests/test_console.py、build_dashboard.py、assets/dashboard.js、看板操作台.py、console.html、看板操作台.spec、打包Mac应用.command、README.md、使用说明.md、PLAN.md、变更记录.md |
| 2026-08-08 | 任务9：对内容进行检查，精简软件 | **复查抓到 3 个真缺陷 + 3 项减法。**【B1】`assets/dashboard.js` 里混进了 **2 个裸 NUL 字节**——上一轮把透视表复合键 `rv + "\0" + cv` 写成了真实 0x00 字符而不是转义。前端源码会被原样内联，**每一份生成的 `项目看板.html` 都带 NUL**，`grep` 判定为二进制文件；且 HTML 规范要求分词器把 U+0000 替换成 U+FFFD，等于分隔符在运行时被悄悄换掉。修法不是补转义，而是**把复合键整个换成「行 Map 套列 Map」的嵌套结构**，从根上消掉这类串味风险；另加 `test_no_control_characters_in_frontend_sources` 扫描 `assets/*` 与 `console.html` 的裸控制字符。【B2】`--idle-timeout 0` 时 `/api/selfcheck` 回出 `{"idleTimeout": Infinity}`——`Infinity` 不是合法 JSON，Python 认但**浏览器 `JSON.parse` 直接抛错**，「环境自检」按钮整个失效。改为无限超时回 0，并加断言禁止响应里出现 `Infinity`/`NaN`。【B3】上传副本的删除放在最外层 `finally`，**排在响应发出之后**，调用方拿到响应时文件可能还在，用户数据副本有可观察的残留窗口（测试也因此偶发翻红）。收紧到 `build()` 一返回就删再发响应，连跑三轮稳定。B1 与 B3 都是上一轮自测通过、实际有问题的代码：B1 因分隔符前后一致所以功能正常，B3 因时序凑巧所以测试变绿。【减法1】spec 新增 `EXCLUDES`：`ssl`/`_ssl`（libssl 632KB）、`_hashlib`（libcrypto 3.5MB）、`lzma`/`bz2`/`_zstd`、`tkinter`、`unittest`/`doctest`/`pydoc`；预计 23MB → 19MB 上下（待 Mac 实测）。依赖链已查清：`ssl` 来自 `urllib.request` 顶部的 `try: import ssl`，`libcrypto` 来自 `random`→`hashlib`→`_hashlib`；**`hashlib` 必须保留**（`random.seed(str)` 用它），只能排 `_hashlib`，它会自动退回内建 `_md5/_sha1/_sha2`。新增 `PackagingExcludesTest`：从 spec 里读出清单、逐个 block 掉再跑完整生成流程验证——以后谁用上了被排除的模块，**测试先红，而不是等打包完用户双击才炸**。【减法2】删除 `打包Windows应用.bat`，spec 去掉 `KANBAN_ONEFILE` 双分支（原打算零成本保留，但两条路径都要维护，且文档还在告诉用户「产物为 dist/看板操作台.exe」）。【减法3】打包脚本构建后自动 `rm -rf build dist/看板操作台`——PyInstaller 的 onedir 目录与 .app 内容重复，每次构建少留约 30MB。顺带清掉 `tests/test_console.py` 里未使用的 `threading` 导入与 `__pycache__`，修正 `使用说明.md` 里丢失的苹果菜单字符。**验收：测试 183 → 188 项，全绿。** | assets/dashboard.js、看板操作台.py、看板操作台.spec、打包Mac应用.command、tests/test_console.py、README.md、使用说明.md、PLAN.md、变更记录.md、（删除）打包Windows应用.bat |
| 2026-08-10 | 任务：接入 GitHub Actions 工作流 | 新增 `.github/workflows/ci.yml`：push main / PR 自动跑 unittest 自检 + 看板生成校验（macos-latest arm64，Python 3.14 + openpyxl 3.1.5）。新增 `.github/workflows/release.yml`：推送 `v*` 标签或手动触发时，自动构建 `看板操作台.app` → `--smoke-test` → 打 `看板操作台_Mac.zip` → 上传产物并创建 GitHub Release（PyInstaller 6.22.0 + openpyxl 3.1.5）。仓库已推送至 GitHub（Xidealsss/offline-dashboard-project），首次提交信息为「项目管理离线看板」，v3.1.0 标签已同步。 | .github/workflows/ci.yml、.github/workflows/release.yml、变更记录.md |
