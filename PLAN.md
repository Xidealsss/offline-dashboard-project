# 离线飞书式项目看板：可行性评估与实现方案

## 1. 结论摘要

**可行，无阻塞性风险。** 在已确认的约束下（自包含 HTML 看板、只读、16 字段进阶模板），五项功能全部可用成熟技术实现，预计 **3～5 个工作日** 完成 MVP：

| 功能 | 可行性 | 实现方式 |
| --- | --- | --- |
| 指标卡 | 高（已有雏形） | 规则引擎聚合 + HTML 卡片，扩到 8 张 |
| 切片器 | 高 | 原生 JS 按钮组（chips），全组件联动 |
| 透视表 | 高 | 原生 JS 行列聚合引擎，数据量 ≤500 行无压力 |
| 项目统计柱状图 | 高 | 内联 SVG 自绘，不依赖任何外部图表库 |
| 甘特图 | 高（已有主体） | 沿用现有实现，适配 16 字段并增加阶段分组/状态配色 |

主要风险是“离线自包含”限制，对策是**全部用原生 JS + 内联 SVG，不引用 CDN/外部库**。

## 2. 关键实现方案

### 2.1 架构与数据流

沿用现有模式：`Excel 16字段模板（唯一数据源）→ build_dashboard.py（解析+校验+计算）→ 单个自包含 项目看板.html`。看板只读，改 Excel 后重新生成；数据以 `__DATA__` JSON 内嵌进 HTML。

### 2.2 页面布局

单页顶部依次为：指标卡行 → 切片器行 → 柱状图区 → 透视表区 → 甘特图区。切片器状态为全局唯一数据源，**所有卡片、图表、透视表、甘特图同时响应**，与飞书仪表盘联动行为一致。

### 2.3 五项功能规格

- **指标卡（8 张）**：总任务数、已完成、进行中、待评审、阻塞、拖期数、里程碑完成率（已完成里程碑/里程碑总数）、本周到期数（计划结束在本周且状态≠已完成）。全部随切片器筛选实时变化。
- **切片器（6 组）**：阶段、负责人、协作部门、状态、风险等级、拖期状态（正常/预警/拖期，由规则引擎计算）。支持多选和“全选/清空”；负责人按“任务包含任一选中人”匹配。
- **柱状图（3 张，内联 SVG）**：各阶段任务数（阶段→计数）、各阶段平均进度（阶段→平均进度%）、负责人待办负载（负责人→计数，状态≠已完成）。空数据显示占位提示。
- **透视表（原生 JS 引擎）**：行维度可选 阶段/负责人/协作部门/优先级/风险等级；列维度可选 状态/阶段/优先级/风险等级/里程碑；值可选 任务计数/平均进度%/总工期（天）；默认视图为“阶段 × 状态 × 计数”，带合计行/列，支持横向滚动。
- **甘特图**：按阶段分组显示任务（显示名=任务+子任务），里程碑菱形、依赖箭头、今日线、周末底色保留；状态配色 已完成绿/进行中蓝/待评审橙/未开始灰/阻塞紫/拖期红/预警黄；依赖箭头仅在无筛选时显示，避免悬空箭头。

### 2.4 规则引擎（统一口径）

计算状态优先级：进度=100% → 已完成；否则 计划结束<今天 → 拖期；否则 距结束≤预警天数且进度<80% → 预警；否则 状态=阻塞 → 阻塞；否则 状态=待评审 → 待评审；否则 进度>0 → 进行中；否则 未开始。

“今天”取**打开看板时的系统日期**（更像飞书 TODAY()），支持 URL 参数 `?today=YYYY-MM-DD` 覆盖，用于回归测试。

## 3. 接口与交付物

- 命令接口：`python3 build_dashboard.py --input 家电产品项目_任务数据.xlsx --output 项目看板.html [--emit-json]`；新增 `运行看板.command` 一键生成并打开。
- 校验规则（沿用并扩展现有脚本）：必填项检查、日期格式、进度 0～100、状态/风险等级/里程碑枚举、任务ID 唯一、前置任务存在、**依赖无环**、项目起止日期合法；错误定位到具体行/列。
- 新增目录 `离线看板进阶/`，交付 `build_dashboard.py`、`家电产品项目_任务数据.xlsx`（16 字段模板副本）、生成的 `项目看板.html` 及 README；现有 `demo/` 保持不变。

## 4. 测试与验收

- **功能验收**：8 张卡片与手算计数一致；任一切片器筛选后卡片/图表/透视/甘特同步变化；透视表切换行、列、值后数字正确；3 张柱状图数值与透视表计数一致；甘特 16 条任务、4 个里程碑、依赖箭头全部正确显示。
- **规则回归**：用 `?today=` 模拟：把某任务计划结束改为昨天 → 甘特标红、拖期卡+1；进度改 100% → 转绿；距结束≤5 天且进度<80% → 预警；状态=待评审 → 显示待评审卡。
- **离线验收**：断网并以 `file://` 直接打开 HTML，确认无外部请求、无 CDN 引用、全部功能可用；重新生成后文件可复制给他人使用。
- **口径验收**：同一份数据下，本地卡片数字与飞书仪表盘对应组件计数一致（文档注明“已完成”本地以进度=100% 判定、飞书公式以状态=已完成 判定的差异）。

## 5. 假设与默认

- 单机离线、只读展示；不做看板内编辑回写、不做多人并发、不做登录权限。
- 数据源固定为 16 字段模板（阶段/任务/子任务/负责人/协作部门/风险等级等）；不支持 11 字段旧模板自动兼容。
- 数据量目标 ≤500 行，原生 JS 渲染足够；饼图、周报、AI 增强、飞书同步不在本次范围。
- 预警天数取自模板「项目配置」页（当前示例为 5 天），可随项目调整。

---

## 6. 后续演进（2026-08-08 完成）

本节记录 MVP 之后围绕「能力与稳定性」做的第二轮工作，详见 `变更记录.md`。

| 阶段 | 内容 |
| --- | --- |
| 一 | 前端拆出 `assets/`，构建时内联；错误模型重构为收集式，退出码分档 |
| 二 | 建立 `tests/` 测试基线（标准库 unittest + 可选 playwright 浏览器回归） |
| 三 | 输入护栏（跨度/行数/公式单元格/环检测迭代化）、选项字典与一致性校验、看板内提示 |
| 四 | 关键路径与浮动时差、甘特日/周/月缩放、搜索与 URL 状态 |

与原 PLAN 的差异：

- 测试框架用**标准库 unittest** 而非 pytest——本项目定位是「双击就能跑」，不宜再加装依赖；
- 数据规模仍按 ≤500 行设计，**只加护栏不做虚拟滚动**（甘特缩放顺带缓解了大项目的渲染压力）；
- 关键路径按**日历日**推算；「工作日」配置仍只作展示。按工作日推算会连带影响工期、
  条形宽度与预警判定，属于另一个量级的改动，留作后续；
- 导出 CSV 与打印/PDF 排版本轮未做。

---

## 7. V3.0 本地操作台（2026-08-08 完成）

本节记录「本地操作台」第三轮工作：把看板工具从“脚本 + 双击命令”升级为可分发到他人电脑的
独立应用，详见 `变更记录.md` 的 V3.0 条目。

| 内容 | 说明 |
| --- | --- |
| 网页式操作台 | `看板操作台.py` 启动仅绑定 127.0.0.1 的本地 HTTP 服务并自动打开浏览器；选择 Excel → 生成/分析 → 打开/下载看板 → 环境自检 → 退出 |
| 复用既有引擎 | 解析/校验/CPM/渲染全部复用 `build_dashboard.py`，未改动其逻辑 |
| Mac 应用打包 | `看板操作台.spec` + `打包Mac应用.command` 产出 `dist/看板操作台.app` 与 `看板操作台_Mac.zip`，`--smoke-test` 验证冻结包 |
| Windows 预留 | `打包Windows应用.bat` 已写好（同一 spec，`KANBAN_ONEFILE=1` 出 onefile exe），需在 Windows 机器上执行，本次未运行 |
| 归档 V3.0 | `离线看板进阶/V3.0/` 独立快照：完整源码 + 构建出的 Mac 应用包 + zip；V2.0 原样保留 |

与原 PLAN 的差异：

- 操作台做成**网页式**（浏览器本地页）而非原生桌面窗口，双平台 UI 一致、开发量小；
- PyInstaller **不支持跨平台转换**：exe 不是从 .app 转出来的，而是在 Windows 上重新打包；
- 未做代码签名/公证，macOS Gatekeeper / Windows SmartScreen 通过「右键 → 打开」「更多信息 → 仍要运行」处理，签名列为后续增强；
- 仍保持完全离线、单机、只监听 127.0.0.1，不引入在线协作与权限体系。

---

## 8. V3.1 迭代路线（2026-08-08 立项 / 调整 / 首轮落地）

功能面在 V3.0 已经收口，本轮不加新功能。**主线是看板的「规模与性能」**（8.2～8.4），
Mac 应用作为支线，**只修硬伤 + 补齐发布规范**（8.5）。**Windows 分支本轮下线**（8.6）。

### 8.1 现状盘点（截至 2026-08-08）

盘点文档与磁盘实际状态时发现几处此前未入账的偏差：

| 项 | 现状 | 处理 |
| --- | --- | --- |
| 分发物落后于最新构建 | `交付物/看板操作台.app`（17:46，内嵌 Python 3.12，主程序 2,365,856 字节）比 `dist/看板操作台.app`（22:53，Python 3.14，2,674,192 字节）旧一轮；两个 `看板操作台_Mac.zip` 也不同（10.98MB / 10.58MB） | 见 8.5 的 R3、R5，用一键发布脚本从根上消灭 |
| 快照 zip 落后于工作目录 | `离线看板进阶_V3.0.zip`（17:48）里的 `项目看板.html` 是 17:02 版，工作目录里是 22:54 版；zip 本身 28 个源码文件、正确排除了 `dist/`、`build/`、`.build-venv/` | 同上，收尾顺序固化进脚本 |
| **app 是 arm64 单架构** | 两个 `.app` 的主程序均为 `Mach-O 64-bit arm64`，**Intel Mac 上双击只会闪退，没有任何提示**。根因是构建用的 Python 只有 arm64（universal2 需换 python.org 安装包） | 本轮**不做 universal2**：只在打包脚本与文档里写明限制，并给出中文提示，见 8.5 的 M4 |
| 构建用 Python 版本会漂 | `.build-venv` 跟着系统 Python 走，这次从 3.12 漂到了 3.14，直接导致上面两版 app 不一致 | 见 8.5 的 R4，锁定并校验 |
| 版本号没有单一来源 | `Info.plist` 里 `CFBundleShortVersionString` 是 PyInstaller 默认的 `0.0.0`；版本还散落在目录名、`server_version = "KanbanConsole/1.0"` 和各文档标题里 | 见 8.5 的 R1 |
| 图标是默认图标 | `看板操作台.spec` 里 `icon=None`，实际用的是 PyInstaller 自带的 `icon-windowed.icns` | 见 8.5 的 R2 |

### 8.2 主线：看板的规模与性能——六项瓶颈

目标：把容量上限从「≤500 行设计 / 800 行护栏」提到 **2000 行 × 5 年跨度**，且在该量级下仍然跑得动。
瓶颈均已定位到具体函数：

| # | 瓶颈 | 位置 | 现状 | 改法 |
| --- | --- | --- | --- | --- |
| 1 ✅ | 透视表逐格全表扫描 | `dashboard.js::renderPivot` | 每个单元格一次 `list.filter`，行合计、列合计再各扫一遍；维度去重用 `rows.includes` 线性查找。「负责人 × 阶段」在 800 行下约 40×10 格 × 800 次比较 ≈ 32 万次/重绘 | 单遍 groupby 进 `Map`，行列合计同一趟累加；去重改 `Set` |
| 2 | 甘特全量重建 | `dashboard.js::renderGantt` | 每次 `refresh()` 都 `innerHTML = ""` 后重建全部任务行，切片器、搜索、展开详情都触发 | 纵向虚拟滚动：行高已收敛为 `--row-h`，只渲染视口 ±N 行；已有的「保留滚动位置」逻辑要一并适配 |
| 3 | 日视图刻度 DOM 量 | `dashboard.js::buildHeader` | 日视图逐日成格，1095 天约 1250 个表头节点、时间轴宽 30660px（`pxPerDay: 28`） | 刻度按视口窗口分段渲染；现有 `headerCache` 从「按粒度缓存」扩成「按粒度 + 窗口缓存」 |
| 4 ✅ | Excel 双读 | `build_dashboard.py::build` | `load_workbook` 开两次（`data_only` 真/假各一次），未用 `read_only`、未显式 `close()` | 改成**惰性**第二次读取（新增 `CachedWorkbook`）：只有真撞上公式单元格才开第二份，并显式 `close()`。**`read_only=True` 实测只省 0.07s / 2000 行（15%），却会踩「同一张表二次迭代」的坑，权衡后没做**，理由记在这里免得下次重新纠结 |
| 5 | 产物体积 | `build_dashboard.py::data_script` | 每任务 26 个字段全量内嵌，含 6 个 CPM 日期字符串（`es/ef/ls/lf` 等） | 日期改存「距时间轴首日的天数」整数、字段名短化，前端解包 |
| 6 | 护栏阈值 | `MAX_ROWS=800` / `MAX_SPAN_DAYS=1095` | 目前是靠护栏「挡住」大项目，而不是「扛住」 | 1～4 项达标后再放宽到 2000 行 / 1825 天，`--max-rows`、`--max-span-days` 保持可调 |

### 8.3 主线执行顺序与实测数字

1. ✅ **性能基准**：`tests/test_perf.py`（合成数据 200 / 500 / 800 / 2000 行 × 1 / 3 / 5 年）。
   默认只做宽松的回归护栏；`KANBAN_PERF_TARGET=1` 切到 8.4 的验收线，没达标就是红的；
2. ✅ 第 1、4 项：纯算法与 IO 改动，无行为变化，风险最低，已落；
3. ⏳ 第 2、3 项：渲染结构改动，必须由 playwright 回归护住几何对齐、冻结列/表头、滚动位置保持；
4. ⏳ 第 5 项：payload 格式前后端同改，产物字节必然变化，不能再用「拆分前后字节一致」那类断言，改为断言解包后数据等价；
5. ⏳ 第 6 项：放宽阈值后复跑基准，达标才提交。

**实测（容器内 Chromium，取隔离运行的数字；跟着整套测试一起跑时浏览器指标波动可达 2～3 倍）**

| 规模 | build | render | HTML | 首屏 | 切片器（最坏） | 切透视维度 |
| --- | --- | --- | --- | --- | --- | --- |
| 200 行 × 1 年 | 0.05s | 0.001s | 0.16MB | — | — | — |
| 500 行 × 3 年 | 0.12s | 0.004s | 0.32MB | — | — | — |
| 800 行 × 3 年 | 0.19s | 0.007s | 0.49MB | — | — | — |
| **2000 行 × 5 年** | **0.56s**（改前 1.02s） | 0.017s | 1.15MB | 780ms | **406ms**（改前 365ms） | **4ms**（改前 1246ms） |

三点值得记下来：

- **瓶颈 4 的收益兑现了**：build 从 1.02s 降到 0.56s，正好是省掉的那一遍文件读取；
- **切透视维度从 1246ms 掉到 4ms**，但主要功劳不是聚合改写，而是发现「切透视维度会连带走一遍
  完整 `refresh()`、把整张甘特重建一遍」——改成只重画透视表就够了；
- **切片器几乎没动（365 → 406ms，在噪声范围内）**。这说明筛选的开销压根不在透视表，而在
  `renderGantt` 全量重建 2000 行 × 6 个 DOM 节点。**要压到 200ms 必须做瓶颈 2 的虚拟滚动**，
  第 1 项优化在这条路径上帮不上忙——这是基准跑出来的结论，事先猜不到。

**「切片器」这个指标本身也修过一次**：最初的写法是点一个阶段 chip 就计时，但那是「筛下去」，
剩的行反而更少，测出来只有 92ms。真正的最坏情况是**取消筛选、2000 行全量回来**那一次
（406ms）。基准写错了比没有基准更危险，这一条留给后来人。

### 8.4 主线验收

- **性能**：2000 行 × 5 年下，首屏 ≤ 2s、任一切片器点击到重绘完成 ≤ 200ms、HTML ≤ 3MB，且逐项对照改动前基线；
- **回归**：既有 120 项测试全绿；浏览器回归的几何对齐、类名撞车守卫、规则引擎矩阵、5 档响应式、零 console error 一项都不放宽；
- **行为不变**：同一份 16 字段数据在改前改后，8 张指标卡数字、透视表默认视图、关键路径条数逐项一致；
- **离线硬约束不变**：`external_refs()` 仍为空，虚拟滚动一律自己写，不得引入任何外部库。

### 8.5 支线：Mac 应用——只修硬伤 + 发布规范

不做体积裁剪、不做 universal2、不做 Mac 原生特性（拖拽生成、菜单栏图标等），这些留到 V3.2 再议。

**M 类：运行期硬伤（四项，都是当前双击就能撞上的）**

| # | 问题 | 根因 | 改法 |
| --- | --- | --- | --- |
| M1 ✅ | Dock 图标点不动、Cmd+Q 杀不掉；关掉浏览器标签后进程变僵尸，只能强制退出 | app 以 `console=False` 打包会进 Dock，但 `main()` 只跑 `while thread.is_alive()` 循环，没有 Cocoa 事件循环，系统事件无人应答；而唯一的退出入口是页面上的「退出操作台」按钮，标签一关就没了 | ①`Info.plist` 加 `LSUIElement=1`，不占 Dock、不再有点不动的图标；②`console.html` 每 30s 打一次 `/api/ping`，服务端连续 N 分钟收不到心跳就自动 `shutdown()` |
| M2 ✅ | 双击两次起两个实例，两个随机端口、两个标签页，且都往同一个 `~/项目看板操作台/项目看板.html` 写，后写的覆盖前写的 | 启动路径没有单实例保护 | 启动时先读 `~/项目看板操作台/.console.port`：能 ping 通就直接 `webbrowser.open` 已有实例并退出；否则正常启动、写入端口文件，退出时清理 |
| M3 ✅ | 用户的项目数据副本长期留在磁盘 | `_handle_generate` 把上传字节写成 `~/项目看板操作台/_upload.xlsx`，从不清理 | 改用 `tempfile` 并在 `finally` 里删；`项目看板.html` 是有意保留的产物，不动 |
| M4 ✅ | Intel Mac 上双击只闪退，用户不知道为什么 | arm64 单架构（见 8.1） | 启动时检测架构并给中文提示；`打包Mac应用.command` 在产物旁生成说明；`使用说明.md` 首行写明「仅支持 Apple 芯片（M 系列）Mac」 |

**R 类：发布规范（五项，目标是让「交付物比 dist 旧一轮」这类事故不可能再发生）**

| # | 事项 | 做法 |
| --- | --- | --- |
| R1 ✅ | 版本号收敛为单一来源 | `看板操作台.py` 里定义 `__version__`；spec 读它写进 `CFBundleShortVersionString` / `CFBundleVersion`，`server_version` 与操作台页脚也从它取，四处始终一致 |
| R2 ⏳ | 换掉默认图标 | 做一个简易 `.icns` 放进仓库，spec 的 `icon=` 指过去 |
| R3 ✅ | 一键发布脚本 | 把 `打包Mac应用.command` 从「只构建」扩成完整流水线：构建 → `--smoke-test` → 同步 `交付物/` → 重打 `离线看板进阶_V3.0.zip` → 打印版本号与各产物校验和。**任一步失败即中止，不留半成品** |
| R4 ✅ | 锁定构建环境 | 改成**漂移检测**而非硬性指定版本：首次构建把版本记进 `.build-python`，之后变了才拦下来问。原本写死 3.12，但本机是 3.14 且 `_zstd` 已在裁剪清单里（libzstd 不会再进包），强行要求换 Python 是白费事 |
| R5 ✅ | 对齐当前两版产物 | **不用在 3.12 / 3.14 两版旧产物之间做选择了**：R4 锁定版本后跑一次新的 `打包Mac应用.command`，它会自动同步 `交付物/` 与快照 zip，一次性把两边覆盖掉。**需要在 Mac 上执行**——容器里跑不了 PyInstaller |

**支线验收**

| 验收项 | 状态 | 覆盖方式 |
| --- | --- | --- |
| 闲置到点自动退出、有心跳则不退 | ✅ | `IdleShutdownTest` 两例（1 秒 / 8 秒超时实跑） |
| 单实例复用、过期端口文件自清、脏端口文件不崩 | ✅ | `SingleInstanceTest` 三例 |
| 上传副本用完即删（成功与出错两条路径都验） | ✅ | `ConsoleServerTest` 两例 |
| 版本号四处一致、页面不写死版本 | ✅ | `VersionTest` 三例 |
| `LSUIElement` 生效、Dock 无图标、Cmd+Q 不再是问题 | ⏳ | 需在 Mac 上打包后人工确认 |
| `打包Mac应用.command` 全流程、交付物与 dist 一致 | ⏳ | 需在 Mac 上执行 |

自动化测试从 **165 项增加到 183 项，全绿**。（PLAN 早前写的「既有 120 项」是旧数字，
本轮实跑核实后更正。）

### 8.6 本轮不做

- ✅ **Windows 分支已下线并清理干净**：`打包Windows应用.bat` 已删除，spec 里的 `KANBAN_ONEFILE` 双分支也一并去掉（原打算「零成本保留」，但留着就得在两条路径上维护，且文档里还写着「产物为 dist/看板操作台.exe」误导最终用户）；README 与 `使用说明.md` 的 Windows 内容已移除；
- **代码签名与公证**：需 Apple 开发者账号，且不签名时「右键 → 打开」这条路是通的，优先级低；
- **universal2 / Intel Mac 支持**：需重建构建环境且包体积翻倍，等确定了分发对象再说；
- ✅ **app 体积裁剪已落地**（详见 8.7）；
- **CPM 按工作日推算、节假日日历、基线对比**——会连带改工期、条形宽度与预警口径，属另一个量级的改动；
- **导出 CSV、打印 / PDF 排版、周报视图**。


### 8.7 精简（2026-08-08）

一轮复查 + 瘦身，三处**真缺陷**和三项**减法**。

**复查抓到的缺陷**

| # | 缺陷 | 影响 | 修法 |
| --- | --- | --- | --- |
| B1 | `assets/dashboard.js` 里混进了 **2 个裸 NUL 字节**（0x00），来自透视表复合键 `rv + "\0" + cv` 写成了真实字符而不是转义 | 前端源码被原样内联进看板，**每一份生成出来的 `项目看板.html` 都带 NUL**，`grep`/`diff` 判定为二进制文件；HTML 规范要求分词器把 U+0000 换成 U+FFFD，等于分隔符在运行时被悄悄换掉 | 复合键整个换成「行 Map 套列 Map」的嵌套结构，不再拼字符串——维度值里带什么字符都不会串味。另加 `test_no_control_characters_in_frontend_sources` 守卫 |
| B2 | `--idle-timeout 0` 时 `/api/selfcheck` 回出 `{"idleTimeout": Infinity}` | `Infinity` 不是合法 JSON。Python 认，**浏览器 `JSON.parse` 直接抛错**，整个「环境自检」按钮失效 | 无限超时回 0；另加断言禁止响应里出现 `Infinity`/`NaN` |
| B3 | 上传副本的删除放在最外层 `finally`，**排在响应发出之后** | 调用方拿到响应时文件可能还在，用户数据副本存在可观察的残留窗口；测试也因此偶发翻红 | 收紧到 `build()` 一返回就删，再发响应。连跑三轮稳定 |

B1 和 B3 都是上一轮**自测通过、却确实有问题**的代码：B1 因为分隔符前后一致所以功能正常，B3 因为时序凑巧所以测试变绿。

**减法**

| 项 | 做法 | 效果 |
| --- | --- | --- |
| 打包裁剪 | spec 增加 `EXCLUDES`：`ssl`/`_ssl`（→ libssl 632KB）、`_hashlib`（→ libcrypto 3.5MB）、`lzma`/`bz2`/`_zstd`（→ libzstd 2.0MB）、`tkinter`、`unittest`/`doctest`/`pydoc` | **实测 23MB → 17MB（−26%），分发 zip 10.58MB → 8.15MB（−23%）**；三个 dylib 在产物里已完全不存在 |
| Windows 残留 | 删 `打包Windows应用.bat`，spec 去掉 `KANBAN_ONEFILE` 双分支 | spec 从 84 行降到 88→约 60 行有效逻辑，只剩一条路径 |
| 中间产物 | 打包脚本构建完自动 `rm -rf build dist/看板操作台`（PyInstaller 的 onedir 目录和 .app 内容重复） | 每次构建少留约 30MB |

裁剪清单不是拍脑袋定的：`ssl` 之所以进包，是 `urllib.request` 顶部 `try: import ssl`；
`libcrypto` 之所以进包，是 `random` → `hashlib` → `_hashlib`。**`hashlib` 本身必须留着**
（`random.seed(str)` 要用），只能排 `_hashlib`，它会自动退回内建的 `_md5/_sha1/_sha2`。
这一整套由 `PackagingExcludesTest` 把清单从 spec 里读出来、逐个 block 掉再跑一遍完整生成流程验证——
以后谁写了用到 `ssl` 的代码，**测试会先红，而不是等打包完用户双击才炸**。

### 8.8 V3.1 首个正式构建（2026-08-08）

| 项 | 结果 |
| --- | --- |
| app 体积 | **17MB**（构建前 23MB，−26%） |
| 分发 zip | **8,151,753 字节**（构建前 10,577,888，−23%） |
| `libcrypto` / `libssl` / `libzstd` | 产物里**全部不存在** |
| `CFBundleShortVersionString` | `3.1.0`（此前是默认的 `0.0.0`） |
| `LSUIElement` / `LSMinimumSystemVersion` | `true` / `11.0` |
| 交付物 vs dist | 校验和一致 `55f840508f023561`，R5 收口 |
| 中间产物 | `build/`、`dist/看板操作台/` 已自动清理 |
| 冻结包自检 | 通过（否则流水线会在第 3 步中止） |
| 内嵌 Python / 架构 | 3.14 / arm64（仍不支持 Intel Mac） |
| V3.0 整个目录 | 137MB → **87MB** |

**这次构建暴露的脚本缺陷**：第 6 步写成了 `if [ -f "$SNAPSHOT" ]` —— 「快照存在才重打」。
快照此前被清理掉了，于是这一步整个跳过，第 7 步的校验和也跟着**静默少了一行**，
看上去像是成功了。已改成无条件重建 + `[ -s ]` 非空断言，且校验和缺失时明确打印
`!! 缺失或为空` 并给出警告——**少一行比错一行更难发现**。

**仍未做**：代码签名与公证（首次运行仍需右键→打开）、自定义图标（R2）、universal2。
