# 项目看板操作台 · 使用说明

> 适用对象：最终使用成员。完整离线，数据只在本机处理，不联网、不监听局域网。
>
> **运行要求：Apple 芯片（M 系列）Mac。** Intel Mac 双击会直接闪退，暂不支持。

## 1. 拿到包之后

1. 把 `看板操作台_Mac.zip` 解压，得到 `看板操作台.app`；
2. 建议把整个 `.app` 放到固定位置（桌面、文稿或某个项目文件夹都可以），**不要单独拷贝里面的文件**；
3. 首次运行如果出现“无法打开，因为无法验证开发者”提示：右键点 `看板操作台.app` → 选「打开」→ 再点「打开」即可。之后可正常双击。

> 这个应用**不会在程序坞里显示图标**——它的界面就是浏览器里那个页面。

## 2. 日常使用

1. 双击 `看板操作台.app`，会自动打开浏览器操作台页面；
2. 点「选择文件」，选中 16 字段 Excel 模板（如 `家电产品项目_任务数据.xlsx`）；
3. 点「生成并分析看板」，页面显示任务数、里程碑、关键路径和数据提示；
4. 「打开看板」在浏览器查看，「下载 HTML」可得到自包含的单文件看板，方便转发给别人；
5. 生成结果会自动保存到：`用户主目录/项目看板操作台/项目看板.html`；
6. 用完直接关掉浏览器标签就行，操作台闲置十分钟会自己退出，不会留后台进程。
   想立刻退出就点页面底部的「退出操作台」。

> 已经开着一个操作台时再双击图标，只会把浏览器切回原来那个页面，不会重复启动。

## 3. 常见问题

- **提示文件格式不对**：请选择 `.xlsx` 或 `.xlsm` 文件。
- **提示数据错误**：按页面列出的行号和原因修改 Excel 后，重新选择文件生成即可；不会留下半成品看板。
- **公式无缓存值**：用 Excel/WPS 打开该文件并保存一次，再重新生成。
- **环境自检失败**：点操作台里的「环境自检」，把显示内容发给维护人员。
- **怎么关闭**：点页面底部的「退出操作台」，或者直接关掉标签页——闲置十分钟后会自动退出。
- **双击没反应**：本应用不进程序坞，请检查浏览器是否已经打开了操作台页面。若确实没反应，
  确认这台 Mac 是 Apple 芯片（左上角苹果菜单 → 关于本机 → 芯片）。

## 4. 维护与打包（给开发/维护人员）

源码目录中：

- 直接运行：`python3 看板操作台.py`；
- 自检：`python3 -m unittest discover -s tests -t tests -v`，或双击 `运行自检.command`；
- 一键发布：双击 `打包Mac应用.command`。它会按「构建 → 冻结包 `--smoke-test` → 同步 `交付物/`
  → 重打源码快照 → 打印各产物校验和」跑完整条流水线，任一步失败即中止，不留半成品；
- 构建用 Python 版本在脚本里锁定（`PINNED_PY`），版本不符会先问再继续——上一轮就是因为
  跟着系统 Python 从 3.12 漂到 3.14，导致交付物和 dist 内嵌的 Python 对不上；
- 版本号只有一处来源：`看板操作台.py` 的 `__version__`，spec、HTTP 响应头、页面页脚都从它取。

## 5. 已知限制

- 仅支持 Apple 芯片 Mac，不支持 Intel Mac（构建环境只有 arm64，做 universal2 需另换 Python）；
- 未做代码签名与公证，首次运行需要走一次「右键 → 打开」；
- Windows 版本已下线，不再维护。
